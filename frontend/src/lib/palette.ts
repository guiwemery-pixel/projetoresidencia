// Paleta categórica (ordem fixa, validada para daltonismo em claro e escuro).
// Usada como identidade de áreas e séries de gráfico — nunca para status.
export const CATEGORICAL = ['#2a78d6', '#eb6834', '#1baf7a', '#eda100', '#e87ba4', '#008300', '#4a3aa7', '#e34948'];

/** Variáveis CSS das séries (trocam no modo escuro automaticamente). */
export const SERIES = ['var(--series-1)', 'var(--series-2)', 'var(--series-3)', 'var(--series-4)', 'var(--series-5)'];

export const chartTheme = {
  grid: 'var(--grid)',
  axis: 'var(--axis)',
  tick: 'var(--muted)',
  surface: 'var(--surface)',
};
